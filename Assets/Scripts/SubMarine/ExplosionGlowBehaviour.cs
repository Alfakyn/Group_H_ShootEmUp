using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExplosionGlowBehaviour : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    void destroyGlow()
    {
        Destroy(gameObject);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
